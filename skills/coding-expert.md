---
name: coding-expert
description: >
  Software development expert. Use for: writing code, debugging, code review, architecture, 
  refactoring, best practices, design patterns, testing, APIs, databases, devops, CI/CD,
  debugging errors, explaining code, optimizing performance.
compatibility:
  tools: [git, python, javascript, typescript, bash, docker, npm, pip, go, rust, java]
  os: [linux, windows, macos]
---

# Coding Expert

## Quick Help

### Debug Python
```bash
python -m pdb script.py
python -c "import pdb; pdb.set_trace()"
```

### Debug JavaScript
```bash
node --inspect script.js
console.log()
```

### Git
```bash
git status
git diff
git log --oneline -10
git checkout -b branch
git add . && git commit -m "message"
```

### Docker
```bash
docker ps -a
docker logs container
docker exec -it container bash
docker build -t name .
docker run -it name
```

### Python
```bash
pip install -r requirements.txt
python -m venv env
source env/bin/activate
pip list
```

### JavaScript/Node
```bash
npm install
npm run dev
node script.js
```

---

## Write Code

Tell me what you need and I'll write it!
